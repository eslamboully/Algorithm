using System;
// using Algorithm.CustomSorting;
using Algorithm.Sorting;

namespace Algorithm
{
    class Program
    {
        static void Main(string[] args)
        {
            // [1] Selection Sort
            // SelectionSort selectionSort = new SelectionSort();
            // foreach (var index in selectionSort.DoSelectionSort())
            // {
            //     Console.WriteLine(index);
            // }
            
            // [2]
            // int[] UnsortedList = { 10, 4, 8, 6, 7};
            // MergeSort mergeSort = new MergeSort();
            // foreach (var index in mergeSort.DoMergeSort(UnsortedList,0,UnsortedList.Length - 1))
            // {
            //     Console.WriteLine(index);
            // }
            
            // BubbleSort bubbleSort = new BubbleSort();
            // foreach (var index in bubbleSort.DoBubbleSort())
            // {
            //     Console.WriteLine(index);
            // }
            
            // [3]
            // int[] UnsortedList = { 10, 4, 8, 6, 7, 2};
            // QuickSort quickSort = new QuickSort();
            // foreach (var index in quickSort.DoQuickSort(UnsortedList,0,UnsortedList.Length -1 ))
            // {
            //     Console.WriteLine(index);
            // }
            
            // [4]
            // InsertionSort insertionSort = new InsertionSort();
            // foreach (var index in insertionSort.DoInsertionSort())
            // {
            //     Console.WriteLine(index);
            // }
        }
    }
}